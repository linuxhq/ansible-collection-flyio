---
- name: Verify
  hosts: all

  pre_tasks:
    - name: Ensure inventory lists are formatted
      ansible.builtin.set_fact:
        machines_list:
          "{{ machines_list |
              map('combine', {'state': 'absent'}) }}"

  roles:
    - machines
...
